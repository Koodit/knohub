services:
  knohub:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PORT=3000
    volumes:
      - ./lancedb:/app/lancedb
      - ./documents:/app/documents
    restart: unless-stopped
